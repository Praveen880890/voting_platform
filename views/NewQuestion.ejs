<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="./css/style.css" rel="stylesheet">
    <title>Add a New question</title>
</head>
<body class="bg-sky-100 overflow-y-hidden">
    <div class="flex mx-auto text-center justify-center"><%- include('header.ejs') %></div>
    <div class="flex gap-1 mx-10 mt-6 ">
        <span>></span>
        <a href="/elections" class="text-black-800 hover:text-green-600">All Elections</a>
        <span>></span>
        <a href="/elections/<%=id%>/" class="text-black-800 hover:text-green-600">Election</a>
        <span>></span>
        <a href="/elections/<%=id%>/NewQuestion" class="text-black-800 hover:text-green-600">Questions</a>
    </div>
    <div class=" flex text-gray-800 antialiased flex-col overflow-y-hidden">
        <div class=" py-3 sm:w-96 mx-auto text-center">
            <span class="text-2xl font-light">Add a new Question</span>
            <div>
                <% if (messages && messages.error && messages.error.length > 0) { %>
                    <% for(var i=0; i<messages.error.length; i++) { %>
                        <p class="bg-orange-100 my-3 list-none rounded-md px-4 py-2"><%= messages.error[i] %></p>
                    <% } %>
                <% } %>
            </div>
            <div class="mt-4 shadow-md rounded-lg text-left bg-white">
                <form action="/election/<%= id %>/questions/create" method="post" class="px-8 py-6">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                    <label for="question" class="block mt-3 font-semibold">What is the question?</label>
                    <input id="question" name="question" type="text" placeholder="Question" class="border w-full h-5 px-3 py-5 mt-2 bg-sky-100 focus:ring-orange-200  rounded-md" required>
                    <label for="description" class="block mt-3 font-semibold">Description</label>
                    <textarea id="description" name="description" class="border bg-sky-100 w-full px-2 py-1 mt-2 focus:ring-orange-200 rounded-md" placeholder="Description" rows="4" cols="15"></textarea>
                    <button type="submit" class="mt-6 w-full bg-orange-500 text-white py-2 px-6 rounded-md hover:bg-blue-600 ">Add question</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>