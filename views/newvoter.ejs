<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="./css/style.css" rel="stylesheet">
    <title>Create New Voter </title>
</head>
<body class="bg-sky-100">
    <div class="flex mx-auto text-center justify-center"><%- include('header.ejs') %> </div>    
    <br> <div class="relative mx-auto mb-8 w-fit">
            <% if(messages && messages.error && messages.error.length > 0)  { %>
               <% for(var i=0; i<messages.error.length; i++) { %>
                    <br>   <li class="text-white rounded px-40 py-4 bg-red-400"> <%= messages.error[i] %></li>
               <% } %>
             <% } %>
          </div>
          <h2 class="text-2xl text-center font-bold my-5"> Add New Voter From Here</h2>
            <div class="mx-auto py-5 px-3 max-w-md bg-white mt-4 shadow-md rounded-lg text-left">
                    <div class="space-y-6 text-base leading-7 text-ray-600 mx-auto text-center justify-center">
                        <form action="/election/<%= id %>/voters/create" method="post">
                            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                            <label for="voterUnqid" class="text-md w-40 inline-block font-medium leading-5 text-gray-700 w-full">
                             Enter Voter ID
                            </label>
                            <input required type="text" id="voterUnqid" name="voterUnqid" class="bg-sky-100 inline-block rounded-lg shadow-md py-3 px-4 mb-5 text-base leading-6 placeolder-gray-500" placeholder="Enter Unique VoterId/UserName" required><br/>
                            <label for="voterUnqPswd" class="text-md w-full inline-block font-medium leading-3 text-gray-700 ">Enter Password</label>
                            <input required type="password" id="voterUnqPswd" name="voterUnqPswd" class="bg-sky-100 inline-block rounded-lg shadow-md py-3 px-4 mb-5 text-base leading-6 placeolder-gray-500" placeholder="Enter Voter's Password"><br/>
                            <button type="submit" id="submit" class="text-center justify-center mx-auto flex w-fit rounded-lg shadow-lg px-3 py-1 bg-green-400 text-white hover:bg-blue-400">Submit</button>
                        </form>
                    </div>
            </div>
        </div>
    </div>
</body>
</html>