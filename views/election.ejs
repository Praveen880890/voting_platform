<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="./css/style.css">
    <title><%= title %></title>
</head>
<body>
    <div class="cent"><%- include('header.ejs') %></div>
    <div class="my-6 mx-10">
        <div>
            <% if (messages.success) { %>
                <p class=" m-3 list-none rounded-md px-4 py-2"><%= messages.success %></p>
            <% } %>
        </div>
        <div>
            <% if (messages && messages.error && messages.error.length > 0) { %>
                <% for(var i=0; i<messages.error.length; i++) { %>
                    <p class=" my-3 list-none rounded-md px-4 py-2"><%= messages.error[i] %></p>
                <% } %>
            <% } %>
        </div>
        <div class="flex gap-2 py-4 justify-center">
            <h5 class="text-2xl">Your Elections</h5>
        </div>
        <div class="flex-auto">
            <div class="mx-4">  
                <p class="text-orange-400 mx-auto text-center"><%= (election.length==0)?"You have not created any elections yet":"" %></p>
                <ol class="list">
                    <% for (let i=0; i<election.length; i++) { %>
                        <li>
                            <div class="flex items-center w-fit my-2 px-2 py-1 rounded">
                                <a href="/election/<%= election[i].id %>" class="m-2 text-sm text-orange-400 "><%= election[i].elecName %></a>
                            </div>
                        </li>
                    <% } %>
                </ol>
                <div class="bg-orange-400 text-center rounded w-fit px-2 py-1 hover:bg-green-600 justify-center mx-auto text-center">
                    <a href="/election/createNew" class="text-white">+ Create a new election</a>
                </div>
            </div>
            </div>
        </div>
    </div>
</body>
</html>