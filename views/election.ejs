<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="./css/style.css">
    <title><%= title %></title>
</head>
<body class="bg-sky-100">
    <div class="justify-center text-center"><%- include('header.ejs') %></div>
    <h3 class="justify-center text-center text-2xl px-3 py-2 left">Hello, <%= username %></h3>
    <a href="/signout" class="justify-center text-center flex text-right underline text-green-400 hover:text-red-400 px-3"> Signout</a> <br>
    <div class="my-6 mx-10">
        <div>
            <% if (messages.success) { %>
                <p class=" m-3 list-none rounded-md px-4 py-2"><%= messages.success %></p>
            <% } %>
        </div>
        <div>
            <% if (messages && messages.error && messages.error.length > 0) { %>
                <% for(var i=0; i<messages.error.length; i++) { %>
                    <p class=" my-3 list-none rounded-md px-4 py-2"><%= messages.error[i] %></p>
                <% } %>
            <% } %>
        </div>
        <div class="flex gap-2 py-4 justify-center">
            <h5 class="text-2xl">Your elections</h5>
        </div>
        <div class="flex flex-col text-center justify-center">
            <div class="mx-4 flex flex-col justify-center text-center">  
                <p class="text-orange-400 mx-auto text-center"><%= (elections.length==0)?"You have not created any electionss yet":"" %></p>
                <ol class="flex flex-col justify-center mx-auto text-center">
                    <% for (let i=0; i<elections.length; i++) { %>
                        <li class="text-center justify-center">
                            <div class="flex items-center w-fit my-2 px-2 py-1 rounded ">
                                <a href="/election/<%= elections[i].id %>" class="text-black bg-stone-200 text-center rounded w-fit px-2 py-1 hover:bg-green-200 justify-center mx-auto text-center"><%= i+1 %>.<%= elections[i].elecName %></a>
                            </div>
                        </li>
                    <% } %>
                </ol>
                <div class="py-5">
                <div class="bg-green-400 text-center rounded  w-fit px-2 py-1 hover:bg-red-600 justify-center mx-auto text-center">
                    <a href="/election/createNew" class="text-white">+ Create a new elections</a>
                </div>
            </div>
            </div>
            </div>
        </div>
    </div>
</body>
</html>