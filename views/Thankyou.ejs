<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Results page</title>
</head>
<body class="bg-sky-50">
    <div class="text-center justify-center"><%- include('header.ejs') %> </div>
        <div class="flex text-center justify-center mx-auto">
            <div>
                <% if (messages && messages.error && messages.error.length > 0) { %>
                    <% for(var i=0; i<messages.error.length; i++) { %>
                        <p class="bg-purple-100 my-3 list-none rounded-md px-4 py-2"><%= messages.error[i] %></p>
                    <% } %>
                <% } %>
            </div>
            <div class="flex flex-col gap-6">
                <span class="text-2xl font-medium">Thankyou for voting!</span>
                <span class="text-2xl font-medium">Election has not ended yet!</span>
                <span class="text-2xl font-medium">Come back to same page for results after the election ends</span>
            </div>
        </div>
    </div>
</body>
</html>